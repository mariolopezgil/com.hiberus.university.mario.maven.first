pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh 'mvn clean test -f pom.xml'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                 withSonarQubeEnv('SonarQube Scanner') {
                                    sh 'mvn sonar:sonar'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}