pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                echo 'Building..'
                sh 'mvn clean test'
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
                 withSonarQubeEnv('nombre_de_la_instalacion_sonarqube') {
                                    sh 'mvn sonar:sonar'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}